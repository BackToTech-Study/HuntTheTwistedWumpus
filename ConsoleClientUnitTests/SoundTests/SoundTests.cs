using ConsoleClient.Sound;

namespace ConsoleClientUnitTests.SoundTests
{
    [TestClass]
    public class SoundTests
    {
        [TestMethod]
        public void TestMethod()
        {
            // Arrange
            var sound = new Sound();

            // Act

            // Assert
            Assert.Fail("Test should fail because the class is empty");
        }
    }
}
